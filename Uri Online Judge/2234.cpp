#include<bits/stdc++.h>
using namespace std;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  float x, y;
  cin >> x >> y;
  cout << fixed << setprecision(2) << x / y << '\n';
  return 0;
}
