#include<bits/stdc++.h>
using namespace std;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  int a, b, c, d;
  cin >> a >> b >> c >> d;
  if (a == 1)
    cout << 1;
  if (b == 1)
    cout << 2;
  if (c == 1)
    cout << 3;
  if (d == 1)
    cout << 4;
  cout << '\n';
  return 0;
}
