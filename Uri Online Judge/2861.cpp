#include<bits/stdc++.h>
using namespace std;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  int t;
  cin >> t;
  string s;
  for (int i = 0; i <= t; ++i) {
    getline(cin, s);
    if (i != 0)
      cout << "gzuz\n";
  }
  return 0;
}
